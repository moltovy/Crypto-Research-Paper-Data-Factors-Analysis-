# DefiLlama / _raw_parts

This folder preserves the **original multi-part downloads** that were merged into canonical CSVs in the parent folder. Keep them as an audit trail; do not use them directly for analysis (use the merged canonical files instead).

See `../README.md` for the merged files.
